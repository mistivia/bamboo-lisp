# 开发日志

## 2026-07-30：用 Bamboo Lisp 解 Advent of Code 2025

任务：在 `examples/aoc2025/` 下用 Bamboo Lisp 解出 AoC 2025 的全部 12 天题目
（参考实现与输入取自 <https://github.com/mistivia/oj/tree/master/advent-of-code/2025/>），
顺便修解释器的 bug、补标准库。

结果：23 个脚本（12 天，第 12 天参考实现只有第一部分，所以这里也只有第一部分），
答案全部与参考 C 实现一致，release 构建下跑完全部约 28 秒。

### 运行方式

每天一个目录，`input` 从参考仓库拷来，每个部分一个脚本，从标准输入读输入：

```sh
cd examples/aoc2025/01
bamboo-lisp part1.lisp < input
```

`examples/aoc2025/run.sh` 跑全部题目并和已知答案对比（`run.sh 08 10` 只跑指定的
某几天）。

**注意**：默认 `make` 是 debug 构建（`-g`，无优化），比 `make mode=release`
慢 4 倍左右，跑这些例子建议用 release 构建。

### 性能基线

release 构建下，`(while (< i n) (setq s (+ s i)) (setq i (+ i 1)))` 跑 100 万次
约 1.3 秒（约 300 万次 eval/秒，每秒约 100 万次循环迭代）。这个量级决定了有几道题
不能照抄参考实现的暴力解法，见后面各题记录。

### 解释器改动

#### 修的 bug

1. **`(i/ 1 0)` / `(mod 1 0)` 直接把解释器打挂**（SIGFPE，core dump）。
   现在返回 error 对象。`builtins.c`
2. **`mod` 的语义**：原来直接用 C 的 `%`，`(mod -3 100)` 得到 `-3`。本语言自称
   「更像 Common Lisp / Emacs Lisp」，而这两者的 `mod` 都是向下取整（floored），
   `(mod -3 100)` 应为 `97`。改成 floored，并新增 `rem` 保留原来的 C 语义
   （截断取余，符号跟被除数）。`builtins.c`
3. **`filter` 和 `remove` 返回的是谓词的返回值，而不是元素本身**：
   `(filter (lambda (c) (char= c #\a)) (list #\a #\b #\a))` 返回 `(#t #t)`，
   应为 `(#\a #\a)`；`remove` 同理返回一串 `#f`。改成 cons 元素本身。
   （顺带把 `filter` 里几处写成 "map:" 的错误信息改对。）`builtins.c`
4. **`break` / `continue` 穿不过嵌套的 `let`**：
   ```lisp
   (while #t (let ((a 1)) (let ((b 2)) (break))))   ; => Error: function call: unexpected control flow signal
   ```
   原因是尾位置的 `let` 会把整个 body 包成一个闭包、返回一个 tailcall 对象，交给
   trampoline 里的 `lisp_apply` 去求值——于是 body 里的 `break` 就跨过了一层函数
   边界，被 `lisp_apply` 拒绝。其实非尾位置那条路径本来就保留了尾调用：body 的最后
   一个表达式用 `istail=true` 求值，返回的 tailcall 对象是自包含的（函数和实参都已
   求值完毕），可以直接往上传给最近的 trampoline，当前帧照常返回。所以直接删掉
   `istail` 的特殊分支，两种情况走同一条路径，`break`/`continue` 自然穿透，
   尾递归依然是平的（测试里 5000 层 `let` 尾调用不爆栈）。`primitives.c`
5. **`#\(`、`#\)`、`#\"`、`#\;`、`#\#`、`#\空格` 没法写**：读取器的 token 扫描
   遇到这些分隔符就停，`#\(` 只读到 `#\` 就报 "Expect character."。现在字符字面量
   会无条件地把 `#\` 后面那个字符读进来。`parser.c`
6. **`vector-ref` / `vector-set` / `vector-remove` / `vector-insert` 只检查上界、
   不检查负下标**，`(vector-ref v -1)` 会越界读堆内存。补上下界检查，
   `vector-insert` 另外补了 `pos > len` 的检查。`vector.c`
7. **`vector-ref` 里有一次多余的 `new_sexp`**（分配一个对象然后直接扔掉），删掉。
   `vector.c`
8. **跑脚本之后会掉进 REPL**：`bamboo-lisp foo.lisp` 执行完脚本会接着读标准输入
   并打印 `>>>` 提示符——脚本输出后面跟着提示符，而且脚本没读完的标准输入会被 REPL
   当成代码读掉。现在跑完文件就退出。`main.c`
9. **`(lines)` 不能读标准输入**：`read-line` 省略参数时读 stdin，`lines` 却强制
   要求一个 stream 参数。统一成可省略。`io.c`

#### 新增 builtin（`builtins.c`）

字符串这块原来只有比较、`split-string`、`strip-string`，没法按下标取字符，
写网格题基本没法下手，补齐：

- `(string-length s)`、`(string-ref s i)`、`(substring s start [end])`
- `(string->list s)`、`(list->string chars)`
- `(string->number s)`：整数返回整数、小数返回浮点，解析失败返回 `nil`
- `(number->string n)`
- `(rem a b)`：见上面 bug 2

#### 新增 prelude（`prelude.lisp`）

- `(dolist (x lst) body...)`、`(dotimes (i n) body...)`：游标 / 计数器在 body
  之前就推进，所以 body 里写 `continue` 不会死循环，`break` 也正常；计数器放在
  隐藏变量里，body 改循环变量也不会打乱迭代。
- `(sort lst pred)`：稳定归并排序，`pred` 是「严格小于」。附带 `merge-sorted`。
- `(list->vector lst)`、`(vector->list vec)`

#### 其他

- `(make-vector)` 现在可以带参数：`(make-vector n)`、`(make-vector n fill)`，
  省掉「循环 append n 次」的样板。`vector.c`
- 上面每一项都在 `tests/` 里补了断言（`arithmetic.lisp`、`string.lisp`、
  `list.lisp`、`vector.lisp`、`char.lisp`、`control-flow.lisp`、`tailcall.lisp`），
  `make test` 的两遍（含 `_alwaysgc` 那遍）全过。
- `docs/en/`、`docs/zh/` 里的标准库和读取器语法文档同步更新。

### 各题记录

答案全部与参考 C 实现的输出一致；下面只记算法上和参考实现不一样的地方。

#### Day 1 表盘（0.07 秒）

直接模拟。`mod` 改成 floored 之后，往左转越过 0 的回绕不用特殊处理。
第二部分参考实现是分情况 `if` 累加，这里直接算「经过 0 几次」：右转是
`(i/ (+ dial amount) 100)`；左转第一次撞上 0 是 `dial` 格（正好停在 0 上也算），
之后每 100 格一次，从 0 出发时第一次要走满 100 格。

#### Day 2 无效 ID（0.01 秒）

参考实现在区间里逐个数试除，输入的区间合计约 215 万个数，树遍历解释器扛不住。
改成**构造**而不是筛选：

- 第一部分，前后两半相同的 2d 位数就是 `block * (10^d + 1)`，`block` 取遍 d 位数。
  它随 `block` 单调，所以落在某个区间里的 `block` 是一段连续区间，贡献是等差数列
  求和。
- 第二部分是「任意短块重复」。同一个长度下按周期 d 枚举，但集合会重叠
  （`111111` 的周期 1、2、3 都算），于是按**最小周期**拆分：周期恰好为 d 的和等于
  周期 d 的总和减去 d 的所有真因子对应的和（因子格上的容斥）。

#### Day 3 最大子序列（0.09 / 0.14 秒）

「保持顺序取 k 个数字使结果最大」是经典单调栈：来了更大的数字就把栈里的旧数字弹掉，
前提是剩下的数字还够填满 k 个。第一部分 k=2、第二部分 k=12，同一份代码换个常数。
参考实现第二部分是回溯搜索所有并列最大值的分支，这里 O(n) 一遍扫完。

#### Day 4 抬纸（0.6 / 2.3 秒）

网格存成一维 vector，外面加一圈 `.` 边框，邻居访问就不用判边界。

第二部分参考实现是「反复整张图扫描直到某一轮没有变化」。抬掉一张纸只会让邻居更容易
被抬走，这个过程单调、不动点唯一，顺序无所谓；于是改成工作表（worklist）：抬掉一张
纸只需要重新检查它的 8 个邻居。

#### Day 5 ID 区间（0.3 / 0.02 秒）

第一部分逐个 ID 查区间；第二部分按左端点排序后合并求并集大小，和参考实现一样。
（注意合并时只合并真正重叠的区间，`1-5` 和 `6-9` 分开算，并集大小不受影响。）

#### Day 6 竖着的数字（0.003 秒）

第二部分每个字符列是一个数字（数字上下叠着写，空格跳过），最后一行是运算符，
从右往左扫，遇到运算符就把攒下来的数字折叠掉。各行长度不一样，要先用空格补齐到
等宽——行尾空格是有意义的，所以读进来不能 strip。

#### Day 7 分光（0.05 / 0.07 秒）

第一部分每行只需要「被光束占据的列的集合」，用一个 bool vector 表示。

第二部分参考实现是带 memo 的递归（深度约 141 层，接近解释器 2048 层的 eval 深度
上限），改成自底向上扫一遍：`below[x]` 表示从下一行的第 x 列出发有多少条路径。

#### Day 8 三维接线（4.4 / 6.6 秒）

999 个接点、约 50 万个点对，排序是不可能的（Lisp 里归并排序 50 万个元素要几百万次
funcall）。

- 第一部分只需要最短的 1000 条边：点对流式过一遍，用一个大小固定为 1000 的大顶堆，
  堆满之后只有比堆顶更短的边才进堆。之后连通块用显式栈 DFS（999 层递归会超解释器
  的深度上限）。
- 第二部分「一直接最短的线直到全连通」就是 Kruskal，而完成连通的那条线正好是最小
  生成树里最长的边。于是用 Prim 建最小生成树（O(n²)，不排序、不存边表），记下最长
  的那条边。

#### Day 9 最大内接矩形（0.6 / 2.3 秒）

第二部分：瓦片按输入顺序构成一条闭合的直角折线，用奇偶规则求出每一行/列在多边形
内部的区间（自身的折线段 + 竖边第 1、2 次穿越之间、第 3、4 次之间……），矩形合法
就是四条边各自落在某个区间里。

参考实现对包围盒的全部约 98000 行和 98000 列都算一遍；而能被问到的只有含瓦片的
248 个不同行和 248 个不同列，所以只算这些。

#### Day 10 按钮（1.9 / 10.8 秒）

第一部分：按钮按两次等于没按，所以解是按钮的**子集**，指示灯是异或。参考实现枚举
2^n 个子集，这里用「到达每种灯态最少要按几次」的 DP，按钮一个个折进去。

第二部分是每台机器解一个整数线性规划：最小化 `sum x`，约束 `A x = b`、`x >= 0`
且为整数，其中 A 的第 i 列是第 i 个按钮接的端口。做法仍是高斯-约当消元把主元变量
表示成自由变量的函数，然后枚举自由变量，但有两点不同：

- 消元全程用整数行变换（每行留着自己的主元分母，再按行内所有元素的 gcd 约一次），
  不需要有理数运算。
- 自由变量的上界靠「主元变量不能为负」反复传播收紧到不动点。参考实现是让每个自由
  变量从 0 跑到 max(b)、用分数算，合计约 2900 万个枚举点；收紧之后只剩约 15 万个，
  相差约 200 倍。这是全部题目里最慢的一道。

#### Day 11 数路径（0.04 / 0.1 秒）

参考实现是带 memo 的递归。图是 DAG，这里改成 Kahn 拓扑排序后正向推路径数，完全
不用递归（图的深度远超解释器的递归上限）。第二部分要求路径同时经过 `fft` 和 `dac`，
于是每个节点存 4 个计数（两个标记的组合）。

#### Day 12 拼块面积（0.08 秒）

按面积判断订单是否可能，直接照抄参考实现的判据。参考仓库没有第二部分，这里也没有。
